# Deployment

- Web: Vercel (`apps/web`)
- API + Worker: Render via [`render.yaml`](../render.yaml)

Render blueprint provisions:

- `paper-pilot-api` (web service)
- `paper-pilot-worker` (background worker)
